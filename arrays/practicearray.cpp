#include <iostream>
#include <cstring>
#include <cmath>
#include <iomanip>
#include <ctime>
#include <climits>
using namespace std;
void display(int list[], int n)
{
    for (int i = 0; i < n; i++)
    {
        cout << list[i] << " ";
    }
}
void reverse(int list1[], int n)
{
}
}
int main()
{
    int arr[5] = {1, 2, 3, 4, 5};
    int size = 5;
    display(arr, size);
    cout << endl;
    int st = 0;
    int end = n - 1;
    while (st < end)
    {
        swap(list1[st], list1[end]);
        st++;
        end--;
        display(arr, size);
        return 0;
    }